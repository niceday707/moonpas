# DECISIONS · 설계 의사결정 기록

> 왜 그렇게 만들었는지를 남기는 곳. "무엇을" 은 코드/PROGRESS 에서 보면 됨.

## 2026-05-16 · 문츠 수동 등록은 1차로 "등록 즉시 노출 + 문제시 숨김" 으로 시작

### 결정
학생/교사가 우상단 `+` 버튼으로 등록한 영상은 **검수 없이 즉시 `visible` 상태**로 피드에 노출된다. 문제가 생기면 admin 이 `hidden` 으로 전환하는 사후 대응 구조. 자동 수집 후보는 여전히 `pending` 으로 들어와 인간 검수를 거친다 (3단 구조 자체는 유지).

### 이유
- 1차 구현에서 "검수 큐 → 승인 UI → 노출" 풀 플로우를 만들면 학생 입장에서 등록의 보상이 즉각적이지 않아 등록 자체가 안 일어남. 죽은 피드가 됨.
- 학교 도메인 OAuth + 본인 닉네임 노출이 1차 압력으로 동작 — 익명 인터넷 게시판처럼 무책임한 등록을 일정 부분 억제.
- 사후 숨김은 본인 + admin 두 경로 모두 RLS 가 강제 (`created_by = auth.uid()` OR admin). 학생/교사는 자기 글을 회수할 수 있고, admin 은 어느 글이든 정리 가능.
- 등록 시 제목/설명/닉네임에 한/영 금지어 부분 매칭 1차 필터를 둠 — 통과한 영상도 영상 자체 내용은 사람이 봐야 함이 원칙(주석/문서 명시).

### 트레이드오프
- 부적절 영상이 잠깐이라도 노출될 수 있다. 미성년자 대상 서비스라서 위험. 하지만 등록량이 적은 1차 단계에서는 운영자가 빠르게 따라잡을 수 있다고 판단.
- 등록량이 일정 임계치를 넘으면 곧바로 "선등록 → 자동 노출 보류 + admin 검수 큐" 로 전환 예정 (다음 마일스톤).

### RLS 설계 핵심
- INSERT: `auth.uid() IS NOT NULL AND created_by = auth.uid() AND source = 'manual' AND review_status = 'visible'` — 클라이언트가 source/review_status 를 임의로 위조 불가.
- UPDATE: `created_by = auth.uid()` OR admin — 본인 회수 + admin 전체 관리.
- DELETE: admin 만 — 학생/교사는 회수(hidden)만, 실제 row 삭제는 운영자 권한.

### 자동 수집기와의 관계
- 자동 수집기는 service-role key 로 직접 upsert (RLS 우회) — `source='youtube_auto'` / `review_status='pending'` 으로 저장.
- 두 경로가 같은 테이블을 공유 — 피드 쿼리는 단순히 `review_status IN ('visible','auto_approved')` 만 보면 됨.
- "auto_approved" 는 미래의 자동 승인 채널(예: 학교 공식 채널 등 화이트리스트)을 위한 자리. 1차에선 사용처 없음.

## 2026-05-16 · 문츠 운영 정책 — 수동 등록 + 자동 후보 + 인간 검수의 3단 구조

### 결정
문츠 영상은 다음 두 경로 중 하나로 수집되고, **반드시 사람의 승인을 거쳐야** 학생 화면에 노출된다.

| 경로 | source 값 | 초기 상태 |
|---|---|---|
| 선생님/관리자 수동 등록 | `manual` | 운영 정책에 따라 `approved` 즉시 가능 |
| YouTube API 자동 수집 (`scripts/find-muntz-candidates.mjs`) | `youtube_auto` | 반드시 `pending` — 검수 전 절대 노출 금지 |

상태머신: `pending` → (승인) `approved` / (탈락) `rejected` / (오래된 pending) `archived`.

### 이유
- 학생 500명이 보는 화면이라 **부적절 콘텐츠 0건** 이 최우선. YouTube 의 safeSearch=strict + 자체 키워드 블랙리스트로도 100% 안전을 보장할 수 없으므로, 자동 수집물은 사람이 한 번 더 보는 단계를 반드시 둔다.
- 수동 등록은 등록자가 학교 구성원(선생님/관리자)이라 추가 검수 부담을 줄일 수 있으나, 동일 상태머신을 공유해 운영 일관성 확보.

### 데이터 흐름 (현재)
1. `npm run muntz:find` 실행 → `scripts/output/muntz-candidates.json` (모두 `reviewStatus: "pending"`).
2. (다음 마일스톤) 이 JSON 을 `muntz_items` 테이블에 upsert.
3. (다음 마일스톤) 관리자 페이지 `/admin/muntz` 에서 검수 → `approved` / `rejected` 전환.
4. (현재 mock) /muntz 학생 화면은 `MUNTZ_ITEMS` 정적 배열을 표시 — DB 연결 후 `approved` 만 조회.

### 자동 수집 정책
- **업로드일 ≥ 올해 1/1**: 트렌드성 확보 + 오래된 영상으로 quota 낭비 방지.
- **조회수 ≥ 1,000,000**: 1차 품질 필터. 학생들이 이미 봤을 가능성 있는 영상은 검수 부담이 적다.
- **길이 ≤ 60초**: shorts 본연의 경험. `videoDuration=short`(4분 미만) 만으로는 부족해서 `contentDetails.duration` 직접 검증.
- **embeddable=true**: 그렇지 않으면 iframe 에서 재생 불가 → 후보 자격 없음.
- **madeForKids 제외**: COPPA 대상 영상은 댓글/맞춤 추천 등 제약이 있어 학생용 큐레이션에 부적합.
- **safeSearch=strict + 키워드 블랙리스트**: 영/한 22개. 제목+설명 모두 검사.
- **상위 20개만 저장**: 검수자 부담 일정 유지(매일 20개 정도가 합리적).

### approved 영속성 & pending 정리
- approved 는 자동 만료하지 않는다 — 학생들이 익숙해진 영상이 갑자기 사라지면 신뢰가 깨짐.
- pending 은 누적된다. 다음 마일스톤에서 **14일/30일 이상 pending 인 항목은 `archived` 로 자동 이동** 하는 cron 도입 검토(quota 절약 + 검수 백로그 통제).

### 트레이드오프
- 매일 자동 수집을 돌리면 quota ~1,500 unit/day 소비(여유: 10,000/day). cron 으로 1일 1회면 충분.
- 검수 부담을 사람이 진다 — 자동 승인 옵션은 두지 않는다. 미성년자 노출 위험이 자동화 편의보다 크다.

## 2026-05-16 · 문츠는 모바일 몰입형 fullscreen viewer 를 기본 UX 로 한다

### 결정
/muntz 진입 시 AppShell 의 TopBar/BottomNav 위로 페이지가 화면 전체를 덮는다. 일반 문파스 카드형 페이지 톤(흰 배경, 큰 여백, 메가메뉴 노출)이 아니라 유튜브 Shorts / 인스타 Reels 같은 검은 배경 풀스크린 뷰어를 기본으로 한다.

### 이유
- 학생 사용자가 쇼츠 앱에서 길든 시각 언어(검은 배경 + 세로 영상 + 우측 액션 버튼 + 하단 오버레이 텍스트)에 즉시 접속해야 이질감 없이 소비한다.
- 1차 mock 구현에서는 일반 페이지 레이아웃 위에 9:16 카드를 얹는 방식이었는데, 모바일에서 흰 배경/여백/하단 BottomNav 가 같이 보이며 "쇼츠가 아니라 게시판" 처럼 느껴졌다.
- 메가메뉴/BottomNav 까지 깔아두면 9:16 비디오가 차지할 수 있는 세로 공간이 줄어 영상이 작아지고 더 이상 "쇼츠" 가 아니게 된다.

### 구현 방식
- 페이지가 `fixed inset-0 z-50 bg-black` 로 화면을 덮음. 부모 layout 의 TopBar/main 패딩에 영향받지 않음.
- AppShell 에서 `/muntz` 일 때 BottomNav 자동 숨김 (`FULLSCREEN_VIEWER_PATHS` 화이트리스트). 메가메뉴 구조는 그대로.
- body 스크롤 잠금 + 컨테이너 내부에서만 `snap-y snap-mandatory`. 모바일 주소창 변화 대응을 위해 `100dvh`.
- PC 는 가운데에 폰 모양 프레임 (max-w-420) 을 보여주고 그 안에서 같은 피드가 동작. 데스크톱 사용자도 모바일에서 보일 모습을 미리 본다.

### 트레이드오프
- 라우트가 `/muntz` 인 동안 헤더 메뉴 접근이 불편 — 좌상단 뒤로가기로 명시적 탈출. 메뉴 진입은 다른 페이지에서 하면 되므로 허용 가능한 비용.
- BottomNav 숨김 로직이 페이지별 화이트리스트로 늘어남 — 향후 풀스크린 뷰어가 추가될 경우 `FULLSCREEN_VIEWER_PATHS` 에만 추가하면 됨.

## 2026-05-16 · 문츠 1차 mock 피드

### 영상 호스팅: 자체 저장 없이 유튜브 iframe embed 만 사용
- 학생/교사 콘텐츠 저작권·저장 책임을 학교가 지지 않기 위함. 영상 자체는 작성자 계정의 유튜브에 머무름.
- 도메인은 `youtube-nocookie.com` 채택 — 트래킹 쿠키 최소화 (학생 사용자 대상이라 중요).
- `rel=0&modestbranding=1&playsinline=1` 옵션으로 관련 영상 노출 줄이고 iOS 인라인 재생 허용.

### 문튜브 vs 문츠 분리 유지
- 문튜브: 큐레이션된 16:9 가로 영상 (진로진학·동기부여·학습법·학교소식).
- 문츠: 9:16 세로 쇼츠, "1분" 단위 소비. 카테고리도 학교생활/동아리/도전·챌린지 등 학생 일상 중심.
- 두 코너를 합치면 카드 비율과 소비 패턴이 섞여 둘 다 약해진다는 판단. 메뉴에서도 "문태 미디어 🎬" 하나에 같이 두되 라우트는 분리(`/youtube`, `/muntz`).

### scroll-snap 컨테이너 방식
- 전체 페이지 스크롤이 아니라 **피드 영역 내부 스크롤**에 `snap-y snap-mandatory` 적용.
- 이유: layout 의 TopBar/BottomNav 가 함께 움직이면 어색해짐. 피드 내부 스냅이면 헤더·필터는 항상 보임.
- 높이는 `calc(100dvh - 14rem)` 로 동적 viewport 사용 — 모바일 주소창 자동 숨김에도 대응.

### 카테고리 & 학년 메타 노출
- 카드에 카테고리/학년 칩을 둘 다 노출 — 학년이 다르면 "남의 콘텐츠" 느낌이 강해 필터링 만족도가 떨어졌던 문튜브 1차 운영 경험을 반영.
- 학년 'all' 도 명시적 라벨("전 학년") — 노출 누락처럼 보이지 않도록.

### 자동재생/음소거 보류
- 쇼츠 UX 핵심이지만 IntersectionObserver + iframe player API 연동이 필요 — mock 단계에서 과한 복잡도라 판단.
- DB 연동 단계에서 함께 도입 예정 (스냅 진입 카드만 unmute autoplay).

### 관리자 등록 UI 보류
- 데이터 구조가 굳지 않은 상태에서 등록 폼을 먼저 만들면 스키마 변경 비용이 큼. mock 으로 UI 검증 → 스키마 확정 → 등록 폼 순서.

## 2026-05-16 · 메뉴 재구조화 5섹션

### "재학생" → "학습·진로"
- 기존 "재학생 🎧" 8 항목으로 가장 비대했고, 의미상 "재학생이 쓰는 것" 은 너무 광범위했음.
- 학년/평가/입시/자료처럼 "공부와 진로" 라는 한 축으로 묶이는 것만 남기고, 미디어(문튜브)·일상(쌤 어디계세요?)은 분리.

### "문태교우" 해체 → "문태 미디어" 신설
- 졸업생 전용 메뉴가 메인 메가메뉴에서 한 섹션을 차지하는 비용 대비 활용도가 낮았음.
- 캠퍼스 스토리/인사이트/동문 뉴스 같은 콘텐츠 성격은 "미디어" 로 묶는 것이 더 자연스러움 — 문튜브·문츠도 같은 섹션에 둘 수 있음.
- 동문 자체 정체성은 졸업생 전용 페이지(`/alumni`)와 졸업생 로그인 흐름에서 별도 유지.

### "문태 이벤트" → "문태 생활"
- 이벤트는 한정 기간 행사 어감이 강한데 실제 콘텐츠(생일/꿀팁/뉴스)는 상시 흐름. "생활" 이 의미를 정확히 잡음.

### career_news 는 ComingSoon BoardType 으로 도입
- 신규 라우트 깨짐 방지 — BoardType 추가 + COMING_SOON_BOARDS 등록만으로 메뉴에 안전하게 노출 가능.
- 데이터 소스(외부 진학 뉴스 큐레이션) 확정 전에는 빈 페이지보다 안내 카드가 사용자 신뢰에 유리.
